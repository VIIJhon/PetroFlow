# PetroFlow Cloud Deployment Requirements (Heroku, AWS Lambda, etc.)
# Includes all base requirements + production server + monitoring

# FastAPI and ASGI server (for cloud)
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
gunicorn==21.2.0

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1
asyncpg==0.29.0

# Pydantic for data validation
pydantic==2.13.4
pydantic-settings==2.1.0
email-validator==2.1.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
pyotp==2.9.0
qrcode==7.4.2
authlib==1.3.0

# Redis for caching (or use managed service)
redis==5.0.1
hiredis==3.3.1

# MQTT for IoT (optional: use AWS IoT Core instead)
paho-mqtt==2.1.0

# HTTP client
httpx==0.26.0
aiohttp==3.14.0

# Data processing
numpy==2.4.6
pandas==3.0.3
scipy==1.16.1

# Scientific computing
CoolProp==7.2.0
thermo==0.2.27
pint==0.25.3

# Plotting and visualization
matplotlib==3.10.5
plotly==5.18.0

# Monitoring & Logging
python-dotenv==1.0.0
structlog==24.1.0

# AWS SDK (if deploying on AWS)
# boto3==1.34.0

# GCP SDK (if deploying on Google Cloud)
# google-cloud-storage==2.10.0
